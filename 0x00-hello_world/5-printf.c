#include <stdio.h>
/**
 * main-is the entry point for the program
 * Return: returns zero by default and no-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
