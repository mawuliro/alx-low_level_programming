#include <stdio.h>
/**
 * main-serve as entry point for the program
 * Return: by default returns zero if no errors otherwise return no-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
